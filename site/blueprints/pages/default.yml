title: Default
options:
  preview: "{{ kirby.option('frontendUrl') }}/{{ page.uri }}"

tabs:
  page:
    fields:
      sections: fields/layout
  children:
    label: Sous-pages
    sections:
      pages: 
        template: default